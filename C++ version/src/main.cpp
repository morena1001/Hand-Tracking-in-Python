#include <iostream>

#include "opencv2/opencv.hpp"
// #include "opencv2/highgui.hpp"

using namespace std;
using namespace cv;

int main () {
    
    
    
    // VideoCapture cap (0);
    // Mat frame;

    // namedWindow ("Input", 0);
    // namedWindow ("Output", 0);

    // Ptr<BackgroundSubtractorKNN> knn = createBackgroundSubtractorKNN (1, 400, false);

    // while (1) {
    //     cap >> frame;
    //     Mat outputFrame;
    //     knn->apply (frame, outputFrame);

    //     imshow ("Input", frame);
    //     imshow ("Output", frame);

    //     waitKey(1);
    // }

    return 0;
}